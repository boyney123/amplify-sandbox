version: 1
appRoot: frontend
frontend:
  phases:
    preBuild:
      commands:
        - ls -la
        - npm ci
    build:
      commands:
        - ls -la
        - npm run build
    postBuild:
      commands:
        - ls -la
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*